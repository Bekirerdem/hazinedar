### Development — Stellar testnet (Docker yok, local network kullanmıyoruz)
[development.network]
rpc-url = "https://soroban-testnet.stellar.org"
network-passphrase = "Test SDF Network ; September 2015"

[[development.accounts]]
name = "me" # Keys saved to ./.stellar/identity; testnet'te friendbot ile fonlanır
default = true

[development.contracts]
# Kontratlar eklendikçe buraya eklenecek (client = true → otomatik build + deploy + TS binding codegen):
#   oracle   = { client = true, constructor_args = "--admin me" }
#   treasury = { client = true, constructor_args = "--owner me --agent me --oracle <ORACLE_ID>" }
